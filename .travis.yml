language: node_js
node_js:
  - lts/*
cache:
  directories:
    - node_modules
install: npm ci
script: npm run build
jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        script: npx semantic-release
